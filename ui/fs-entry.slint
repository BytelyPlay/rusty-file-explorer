import { Button } from "std-widgets.slint";
import { CustomButton } from "custom-widgets/custom-button.slint";

export component FsEntry inherits CustomButton {
    width: 100%;
    height: 100%;

    background: green;
    border: black;
    border_width: 1px;

    on_click => {
        debug("Clicked fs entry.")
    }
}